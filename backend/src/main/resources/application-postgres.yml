spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/storycore
    username: storycore
    password: storycore
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql

app:
  demo-user-id: 1